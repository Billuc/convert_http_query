name = "convert_http_query"
version = "1.0.0"

# Fill out these fields if you intend to generate HTML documentation or publish
# your project to the Hex package manager.
#
description = "Encode and decode HTTP queries from/to Gleam types"
licences = ["Apache-2.0"]
repository = { type = "github", user = "Billuc", repo = "convert_http_query" }
links = [{ title = "Website", href = "https://hexdocs.pm/convert_http_query" }]
#
# For a full reference of all the available options, you can have a look at
# https://gleam.run/writing-gleam/gleam-toml/.

[dependencies]
gleam_stdlib = ">= 0.34.0 and < 2.0.0"
convert = ">= 1.0.0 and < 2.0.0"

[dev-dependencies]
gleeunit = ">= 1.0.0 and < 2.0.0"
